---
description: Run the grill relentless-interview router in plain, docs, or sdd mode
argument-hint: "[me|docs|sdd] [topic]"
---
# /grill

Load and follow the `grill` skill.

Select the mode from the first argument:

- `docs` → docs mode.
- `sdd` → sdd mode.
- `me`, anything else, or no mode argument → plain mode.

Treat the remaining arguments as the interview topic. If no topic is provided, ask for the plan, design, code approach, or change to interview about before starting.

All mode behavior, hard rules, decision gates, and write steps come from the `grill` skill; do not restate or override them here.

---
description: Socratic review where the user defends the design decisions in their code
agent: review-coordinator
subtask: false
argument-hint: "[diff, branch, files, or scope to defend]"
---
Raw arguments: `$ARGUMENTS`

Run `operation=defend`. Review read-only, one design decision and user defense at a time; never fix, commit, or push. Ask one blocking question if no scope or working-tree diff exists.

---
description: Inspect backend service inputs and outputs with evidence
agent: architect
subtask: false
argument-hint: "[target path, service, module, or scope]"
---
Raw arguments: `$ARGUMENTS`

Run `operation=boundary`. Inspect one exact target statically; write only its report under `.ai/architect/reports/`. No source edits, shell, web, commit, push. Ask one blocking question when the target is missing.

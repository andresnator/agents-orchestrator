---
description: "Generate or refresh evidence-backed C4-lite architecture docs."
agent: architect
subtask: false
argument-hint: "[optional subpath or focus]"
---
Raw arguments: `$ARGUMENTS`

Run `operation=map`. Write only `<docfolder>/architecture/**`; refresh in place. No source edits, commit, push.

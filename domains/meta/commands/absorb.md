---
description: Analyze external AI harnesses and contrast their practices against this repo
argument-hint: "[git URL(s) or local path(s), optional focus]"
---
# /absorb

Load and follow the `absorb` skill for the provided target(s).

If no git URL or local path is provided, ask for at least one before starting. Pass any remaining arguments as the analysis focus (skills, agents, prompts, workflows, installer patterns, or comparison themes).

Scope, hard rules, the adversarial filter, and the report contract (including writing to `.ai/absorb/YYYY-MM-DD-external-practices.md` when persistence is wanted) come from the `absorb` skill; do not restate or override them here.

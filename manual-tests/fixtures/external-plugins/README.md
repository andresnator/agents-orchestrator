# External plugin JSONC fixtures

Use these before/after pairs only for the human case [`MT-REPOSITORY-EXTERNAL-PLUGIN-LIFECYCLE`](../../../docs/manual-testing.md#mt-repository-external-plugin-lifecycle). Copy a `before` file into a disposable OpenCode target, perform the documented install lifecycle, and compare the result with its matching `after` file while checking that comments and foreign entries remain.

No repository runner reads these files.
